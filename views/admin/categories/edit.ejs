<!DOCTYPE html>
<html lang="pt-br">
<%- include ('../../partials/head.ejs') %>

    <body>
        <%- include ('../../partials/navbar.ejs') %>
            <div class="container d-flex justify-content-center">
                <div class="card w-50">
                    <div class="card-header">
                        <h2 class="text-center">Edição de categoria</h2>
                    </div>
                    <div class="card-body">
                        <form class="mt-2 d-flex flex-column" method="POST" action="/categories/update">
                            <h5 class="text-center">
                                Categoria Atual:
                            </h5>
                            <p class="text-center">
                                <%= category.title %>
                            </p>
                            <input type="text" name="title" placeholder="Edite o novo título da categoria"
                                class="form-control mb-3">
                            <input type="hidden" name="id" value="<%= category.id %>">
                            <button class="btn btn-primary w-25">
                                Atualizar
                            </button>
                        </form>
                    </div>
                </div>
            </div>
            <%- include ('../../partials/bootstrapScript.ejs') %>
    </body>

</html>